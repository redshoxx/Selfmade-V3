export async function compressImage(file,{max=1800,quality=.78}={}){if(!file||!file.type.startsWith('image/'))throw new Error('Bitte eine Bilddatei auswählen.');if(file.size>20_000_000)throw new Error('Das Originalbild ist größer als 20 MB.');const bitmap=await createImageBitmap(file);const scale=Math.min(1,max/Math.max(bitmap.width,bitmap.height));const canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));const ctx=canvas.getContext('2d',{alpha:false});ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(bitmap,0,0,canvas.width,canvas.height);bitmap.close?.();const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg',quality));if(!blob)throw new Error('Bild konnte nicht komprimiert werden.');return new File([blob],`receipt-${Date.now()}.jpg`,{type:'image/jpeg'})}
export async function detectText(file){if('TextDetector'in window){try{const bitmap=await createImageBitmap(file);const blocks=await new TextDetector().detect(bitmap);bitmap.close?.();return blocks.map(block=>block.rawValue).join('\n')}catch{}}return''}
