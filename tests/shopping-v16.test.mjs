import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const app=await readFile(new URL('../public/app.js',import.meta.url),'utf8'),styles=await readFile(new URL('../public/styles.css',import.meta.url),'utf8'),index=await readFile(new URL('../public/index.html',import.meta.url),'utf8');test('shopping page uses the V16 quick-add workflow',()=>{assert.match(app,/Was brauchst du\?/);assert.match(app,/shopping-quick-v16/);assert.match(app,/open-shopping-details-v16/);assert.match(app,/shoppingCatalogMatchesV16/);assert.match(app,/parseShoppingDraftV16/);});test('V16 shopping renderer only displays real data suggestions',()=>{assert.match(app,/const suggestions = data\.suggestions \|\| \[\]/);assert.match(app,/aus deinen Daten/);});test('shopping layout is compact and usable on iPhone 12',()=>{assert.match(styles,/\.shopping-quick-form-v16/);assert.match(styles,/grid-template-columns:\s*minmax\(0, 1fr\) auto/);assert.match(styles,/\.shopping-item-v16/);assert.match(styles,/min-height:\s*65px/);assert.match(styles,/\.shopping-add-actions-v16/);});test('legacy V16 shopping code coexists with the current cache version',()=>{assert.match(index,/app\.js\?v=19\.2/);assert.match(index,/styles\.css\?v=19\.2/);});
