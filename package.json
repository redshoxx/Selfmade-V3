{
  "name": "selfmade-einkauf",
  "version": "2.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "prebuild": "npm test",
    "build": "node build.mjs"
  },
  "engines": { "node": ">=20" }
}
