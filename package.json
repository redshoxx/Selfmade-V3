{
  "name": "selfmade-einkauf",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs"
  },
  "engines": { "node": ">=20" }
}
