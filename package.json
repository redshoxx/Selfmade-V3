{
  "name": "selfmade",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "prebuild": "npm test",
    "build": "node build.mjs"
  },
  "engines": { "node": ">=20" }
}
