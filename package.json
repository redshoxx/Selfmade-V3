{
  "name": "selfmade-einkauf",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
